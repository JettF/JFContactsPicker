platform :ios, '10.0'

use_frameworks!
inhibit_all_warnings!

#This is only to be used when using development pods.
#install! 'cocoapods', :disable_input_output_paths => true

source 'https://bitbucket.org/app-order/zorts-private-spec-repo.git'
source 'https://github.com/CocoaPods/Specs.git'

podspec

target :ZRAboutMyTeamController do
end

target :ZRAboutMyTeamControllerTests do
    pod 'Nimble', '~> 7.3'
end

